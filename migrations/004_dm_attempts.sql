-- Факт ПОПЫТКИ отправки, а не только исход.
-- Зачем: решение «снимать ли бронь на человека» до сих пор выводилось из пустого
-- dm_message_id, а пустой он в двух разных случаях — «не пробовали» и «пробовали, ответ
-- Meta потерян». Второй случай при снятии брони выдаёт человеку материал ВТОРОЙ раз.
-- Явный счётчик разводит эти случаи, не требуя знания кодов ошибок Meta.
ALTER TABLE instagram.ig_delivery ADD COLUMN dm_attempts int NOT NULL DEFAULT 0;
