fastapi>=0.115
uvicorn>=0.30
psycopg[binary,pool]>=3.2
python-dotenv>=1.0
# Исходящие вызовы: Graph API Instagram и Telegram Bot API для аварийных сигналов
httpx>=0.27
# Конверт AES-256-GCM для токена доступа: в базе он лежит зашифрованным
cryptography>=43
# Шаблоны панели и публичных страниц. Единственная новая зависимость этапа, и берётся
# она ради автоэкранирования: тексты правил пишет человек, а рисуются они в HTML.
# python-multipart сюда НЕ добавлен, хотя starlette требует его для request.form()
# даже на обычной urlencoded-форме (проверено живьём: без него 500 на входе в панель).
# Тело формы разбирает parse_qsl из стандартной библиотеки — panel.read_form; заводить
# зависимость ради разбора «a=1&b=2» не за что, а загрузки файлов в панели нет.
jinja2>=3.1
